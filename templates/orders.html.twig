{% extends 'base.html.twig' %}

{% block title %}My orders{% endblock %}

{% block body %}
    <div class="row">
        <div class="justify-content-center align-items-center">
            <div class="d-flex flex-column justify-content-center align-items-center">
                <h1 class="mb-4 text-nowrap">My orders</h1>
            </div>
            <div class="row">
                {% if orders|length > 0 %}
                    <table class="table table-hover text-nowrap table-borderless mt-1">
                        <thead>
                        <tr>
                            <th class="align-middle">Order number</th>
                            <th class="align-middle text-center">Total price</th>
                            <th class="align-middle text-right pr-4">Action</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for order in orders %}
                            {# @var order \App\Entity\Order #}
                            <tr>
                                <td>
                                    {#                                    <a href="{{ path('book_resource', {'id': cartItem.book.id}) }}"> #}
                                    {{ order.orderNumber }}
                                    {#                                    </a> #}
                                </td>
                                {#                                TODO resolve getting the currency correctly #}
                                <td class="align-middle text-center">{{ money_manager.formattedPrice(order_manager.totalPriceForOrder(order)) ~ ' ' ~ order.orderItems.first.book.currency.code }}</td>
                                <td class="align-middle text-right">
                                    {#                                    <a href="{{ path('app_cart_remove', {'book': cartItem.book.id}) }}"> #}
                                    {#                                        <button type="submit" #}
                                    {#                                                class="btn btn-sm btn-danger custom-cart-button">Remove #}
                                    {#                                        </button> #}
                                    {#                                    </a> #}
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                        {#                        <tfoot> #}
                        {#                        <tr> #}
                        {#                            <td class="text-bold align-middle">Total</td> #}
                        {#                            <td class="text-bold align-middle text-center">{{ total ~ ' ' ~ currency }}</td> #}
                        {#                            <td class="align-middle text-right"> #}
                        {#                                <a href="{{ path('app_order_place_order') }}"> #}
                        {#                                    <button type="submit" class="btn btn-sm btn-primary custom-cart-button"> #}
                        {#                                        Order #}
                        {#                                    </button> #}
                        {#                                </a> #}
                        {#                            </td> #}
                        {#                        </tr> #}
                        {#                        </tfoot> #}
                    </table>
                {% else %}
                    <p class="text-center">You haven't placed any orders yet.</p>
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}